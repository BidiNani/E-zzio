repos:
  - repo: local
    hooks:
      - id: check-contracts
        name: "[contracts] validation providers <-> code"
        entry: python scripts/check_contracts.py
        language: system
        pass_filenames: false
        always_run: true
